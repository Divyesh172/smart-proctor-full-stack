# --- Core Framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0

# --- Security & Auth ---
# Used for JWT generation and validation
python-jose[cryptography]>=3.3.0
# Used for password hashing (Bcrypt)
passlib[bcrypt]>=1.7.4
# Required for OAuth2 form handling in Swagger UI
python-multipart>=0.0.9

# --- Database ---
# The standard synchronous PostgreSQL driver
sqlalchemy>=2.0.29
psycopg2-binary>=2.9.9

# --- Utilities ---
# For making HTTP requests to the Go service
requests>=2.31.0
# For loading .env files
python-dotenv>=1.0.1
email-validator>=2.1.1